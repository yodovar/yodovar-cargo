import 'package:flutter/material.dart';

/// Для SnackBar после навигации (токены сохранены, экран закрыт).
final GlobalKey<ScaffoldMessengerState> appMessengerKey =
    GlobalKey<ScaffoldMessengerState>();
